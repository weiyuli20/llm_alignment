约束解码：要求大模型生成的文本满足制定格式的约束，比如生成指定的json, sql, xxx-xxx-xxxx的11位电话号码

以json生成为例：
模型生成时可能会出现json截断，括号引号不闭合，key增加或者减少，value值类型不符合约束（整数，字符串，列表...)

- 依靠提示词工程，不能保证100%的准确性，在接口数据流转的过程中，可能设计到多次json输出，如果失败率为2%，那么连乘起来，整个系统的成功率就会快速下降， 1 * 98% * 98% * 98% *...
- 提示词+ 异常兜底，这场方式需要为每种错误场景增加异常处理，增加了代码复杂度且不能100%保证
- 大模型厂商api调用，原生实现约束解码（openai, anthropic)，但是这种方式锁死了大模型的可用范围，没有模型自由度
- 约束解码:基于语法或状态限制修改大模型输出的token logits 分布，非法的token logits 设置为负无穷
 - 基于状态机的方式：outlines库
 - 基于上次文无法文法（CFG)：xgrammar, llguidance实现了这种方式
 - vllm 可以集成xgrammer实现约束解码


一些使用场景说明：
<img width="641" height="242" alt="image" src="https://github.com/user-attachments/assets/91157cb4-b0d5-40ae-91ae-08e821f89cd1" />

<img width="757" height="343" alt="image" src="https://github.com/user-attachments/assets/bf6ccad8-1816-41e2-8d9a-7e45543a9518" />



