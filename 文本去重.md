文档去重：
- Jaccard
- hash
- simhash
- minhash
- LSH

前置知识：文档分词 、 ngram

假设有两篇文章：
-1. 今天天气很好
-2. 今天天气不好

- 分档分词：
  第一个句子：{今天，天气，很好}
  
  第二个句子：{今天， 天气，不好}

- ngram：将n个词拼在一起

假设为2-gram:
- 第一个句子：{今天天气，天气很好}
- 第二个句子：{今天天气，天气不好}

# Jaccard
统计两个集合的交并比：A与B的交集个数 / A与B的并集长度。

计算可得两个文档的相似度：1/3 = 0.33

# Hash
可以将文档映射为固定长度的hash值

问题：hash性质：（1）雪崩效应：很小的差异导致hash值差距很大，因此不能用于比较文档相似度 

可以基于python内置库生成指纹,hash的底层是加密算法，还有很多别的高性能库可以使用
```
import hash
hash("今天天气很好”）
```


# SimHash

由google提出，适合长文本（长度>500)，SimHash 是局部敏感hash


# minhash

计算步骤：
- 文档分词，合并n-gram   n值5-9
- 对每个n-gram 计算minhash, 这样的操作重复n次，即用n个hash函数计算出n个minhash,得到一个n维的signature
- LSH 相似度计算：对n维指纹进行分段， 每段使用独立hash进行映射，只有两个文档在任意一段 **完全相同** ，映射到同一个桶里，就认为两个文档候选相似

  文本相似 ，minhash向量也相似
  
  依据：两个向量相似，至少有一段完全相同，不相似的向量存在完全相同的片段的可能性很小


假设存在一个文档特征矩阵

- shingling在两个文档都存在的情况标记为X
- shingling在两个文档一边存在的情况标记为Y
- shingling在两个文档都不存在的情况标记为Z

** 找到第一个X（即找到第一个1）的概率为 X /X+Y = JACCARD(A,B) **

理想情况下：可以对特征矩阵做n次排列变化，然后计算n次排列的x/x+y 的期望

只有在哈希函数不存在碰撞这个强约束下，「取集合最小哈希值」才完全等价于置换排序寻找第一个 1；


https://kongfangyu.com/2021/02/19/%E5%A4%A7%E8%A7%84%E6%A8%A1%E6%96%87%E6%9C%AC%E5%8E%BB%E9%87%8D/

但是现实场景中hash碰撞不可避免，所以计算结果近似等于jaccard
  

